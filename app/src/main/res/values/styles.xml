<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

<!--  Custom Style  -->
    <style name="EditTextProject" parent="@style/Widget.AppCompat.EditText">
        <item name="android:background">@android:color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Medium</item>
        <item name="android:elegantTextHeight">true</item>
        <item name="textStartPadding">15dp</item>
    </style>

    <style name="ButtonSubmit" parent="@android:style/Widget.Material.Light.Button">
        <item name="color">@android:color/holo_blue_dark</item>
        <item name="backgroundTint">@color/customColorButton</item>
    </style>

</resources>